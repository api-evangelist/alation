openapi: 3.2.0
info:
  title: Alation Lineage API
  description: REST API for managing data lineage and dataflow objects in the Alation data intelligence platform. Supports creating, retrieving, updating, and deleting lineage paths and dataflow objects.
  version: 1.0.0
  contact:
    name: Alation Developer Support
    url: https://developer.alation.com
servers:
- url: https://{instance}/api/v1
  description: Alation instance
  variables:
    instance:
      default: your-alation-instance.com
security:
- BearerToken: []
tags:
- name: Lineage
  description: Retrieve lineage paths between catalog objects
paths:
  /lineage/:
    get:
      operationId: getLineage
      summary: Get lineage for an object
      description: Returns upstream and downstream lineage paths for a catalog object.
      tags:
      - Lineage
      parameters:
      - name: object_type
        in: query
        required: true
        description: Type of catalog object (table, attribute, etc.)
        schema:
          type: string
      - name: object_id
        in: query
        required: true
        description: ID of the catalog object
        schema:
          type: integer
      - name: direction
        in: query
        description: Lineage direction
        schema:
          type: string
          enum:
          - upstream
          - downstream
          - both
          default: both
      responses:
        '200':
          description: Lineage paths
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LineageGraph'
components:
  schemas:
    LineageGraph:
      type: object
      properties:
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/LineageNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/LineageEdge'
    LineageNode:
      type: object
      properties:
        object_type:
          type: string
        object_id:
          type: integer
        title:
          type: string
        url:
          type: string
          format: uri
    LineageEdge:
      type: object
      properties:
        source:
          $ref: '#/components/schemas/LineageNode'
        target:
          $ref: '#/components/schemas/LineageNode'
        dataflow_id:
          type: integer
  securitySchemes:
    BearerToken:
      type: http
      scheme: bearer
